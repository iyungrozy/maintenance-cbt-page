# maintenance-cbt-page

Static maintenance page served by Nginx.

## Deployment

This repository now includes `/home/runner/work/maintenance-cbt-page/maintenance-cbt-page/docker-compose.yml` for Dokploy or any Compose-based deployment.

### Docker Compose

```bash
docker compose up --build -d
```
